* transmission cli
* other WMs to set up
  - bspwm
  - i3 (no-gaps)
* irssi
  - learn
  - move config files to ~/.config/irssi
  - add alias in zshrc 
    `alias irssi='irssi --home=~/.config/irssi --config=~/.config/irssi/config'`
  - find out if specifying --config is irrelevant after doing --home
* fill in missing unicode
* conky
* global color config
  - Xresources controls all colors
    - (done) terminal
    - (done) rofi
    - (done) neovim
    - i3
    - bspwm
    - openbox
    - firefox
    - startpage (need a startpage first though)
  - make python script with pywal module to reload programs on color change.
* firefox rice
  - move bookmarks into button that expands across the line.
* startpage
* music (mpd, ncmpcpp, mpc), (tizonia)
* limebar
  - figure out how to make interactive dropdowns in general
  - currently running applications (done)
    - apply color to selected application (done)
  - time & date dropdown (time, dropdown is calendar with date)
  - internet dropdown
    - lone glyph of either disconnected wifi, connected wifi or ethernet
    - clicking glyph will open dropdown of all available networks
      - clicking entry in dropdown will connect to that network
      - clicking wifi network while connected over ethernet is a NOP
  - volume (laptop)
    - clicking toggles mute
  - battery (laptop)
    - displays estimated time remaining
    - displays which programs are using most power
* rEFInd desktop setup (boot windows from custom stanza)
* keyboard and mouse colors on linux (desktop)
* zsh plugins
  - syntax highlighting
  - autosuggestions
* update GTK theme and icons
* mail
  - neomutt
* fix weird looking horizontal line in terminal
* switch from rofi to dmenu
* fix st cursor shape (in shell and vim)
* set up Tor and Encrypted DNS
* host mda files somewhere
* allow vim status line to be transparent
  - blocks st transparency since it looks odd with one opaque bar in the middle
    of the screen
